version: '3.8'

services:
  esg-checker:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data/chromadb:/app/data/chromadb  # Persist ChromaDB index
    env_file:
      - .env
    restart: unless-stopped
